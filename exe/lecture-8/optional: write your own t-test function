##################################################
################### t-test ##########################
##################################################
# to calculate the t-test, you need the 
# means and standard deviations, and observed N
# use vectorized commands (work with vectors instead 
# of looping over i)

# create function to conduct t-test on one variable
# group should be variable with 2 levels (1,2)
# use complete names, i.e. including the dataframe
ttest<-function(var, group){
  
  # 1. create groups

  
  # 2. exclude missings

  

  
  # 3. calculate p value

  
  # 4. return values
 
}
ttest(... , ...)


# check with t.test function


